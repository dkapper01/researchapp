extends layout

block content

  h1 #{company.company_name}
    h4
      u Portfolio Company Information

  p #[strong Investment date:] #{company.investment_date}
  p #[strong Bloomberg URL:] #{company.leadership_page_url}
  p #[strong Titanhouse URL:] #{company.titanhouse_url}

  // - var count = 0
  // each val in company_titan
  //     tr
  //         td(class='count_width') #{++count}
  //         td(class='main_width')
  //             a(href=val.url) #{val.titan_name}
  //         td
  hr
  

  p #[strong Delete the following copies before attempting to delete this Company.]



  p Do you really want to delete this Company?
    
  form(method='POST' action='')
    div.form-group
      input#id.form-control(type='hidden',name='id', required='true', value=company._id )

  button.btn.btn-primary(type='submit') Delete